name: Windows Smoke
on: [push]
jobs:
  smoke:
    runs-on: windows-2022
    steps:
      - run: |
          Write-Output "Windows runner operational"
          Write-Output "OS: $([System.Environment]::OSVersion)"
          Write-Output "Host: $env:COMPUTERNAME"
        shell: pwsh