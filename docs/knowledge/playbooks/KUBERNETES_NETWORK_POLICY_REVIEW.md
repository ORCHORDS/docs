# Kubernetes Network Policy Review

Review expected ingress and egress paths, namespace boundaries, DNS requirements, default behavior, and operational exceptions. Validate policy changes against real dependencies.

Sources: OWASP Kubernetes Security Cheat Sheet.