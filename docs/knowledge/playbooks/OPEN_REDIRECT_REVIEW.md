# Open Redirect Review

Review redirect and return-url features for arbitrary external navigation, unsafe scheme handling, and weak allowlists. Prefer route identifiers or validated relative paths where possible.

Sources: OWASP Unvalidated Redirects and Forwards guidance.