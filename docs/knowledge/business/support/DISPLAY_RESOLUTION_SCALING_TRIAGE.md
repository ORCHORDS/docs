# Display Resolution and Scaling Support Triage ## Purpose and scope This runbook isolates display resolution and scaling incidents without weakening managed controls. It covers intake, bounded tests, evidence, restoration, and handoff. Analysts should begin with the display_resolution_scaling_triage-trace smallest harmless comparison and invoke incident management when broad impact, safety, or a critical deadline is involved. The procedure does not authorize invasive inspection, unsupported configuration, or access beyond the support role. ## Intake signals Record display_resolution_scaling_triage-trace monitor topology, native mode, and scaling, exact error text, local time and zone, device and application versions, connection context, first occurrence, frequency, and duration. Determine whether scope is one user, device, location, application, or shared display_resolution_scaling_triage-trace population. Ask what last worked and what immediately changed, treating recollection as a lead rather than proof. Obtain one harmless reproducible action and one genuinely comparable unaffected sample. ## Diagnostic workflow Capture baseline state before retrying. display_resolution_scaling_triage-trace Reproduce once, inspect the nearest boundary, and then move outward one layer at a time. For display resolution and scaling, compare the topic-specific signal against an approved known-good input. Change only one variable, predict its display_resolution_scaling_triage-trace result, record the observation, and restore it before the next test. Correlate client, intermediary, and service records by timestamps or request identifiers; preserve any clock disagreement. [display-resolution-scaling-triage checkpoint] ## Controls and evidence Retain sanitized monitor topology, display_resolution_scaling_triage-trace native mode, and scaling, event codes, timestamps, configuration revisions, comparison outcomes, and every state-changing action. Store artifacts only in the approved case system, redact secrets and unrelated content, and retain before-and-after values. Mark requester statements, display_resolution_scaling_triage-trace observations, inferences, and hypotheses separately. A screenshot or status label proves only the displayed component state; explicitly name the boundary each test demonstrates and preserve useful negative [display-resolution-scaling-triage checkpoint] results. ## Escalation Escalate display resolution and display_resolution_scaling_triage-trace scaling when the symptom reproduces on a matched managed peer, impact reaches the incident threshold, required privilege exceeds support authority, or telemetry identifies an upstream owner. Route physical defects to hardware support and unexpected redirection display_resolution_scaling_triage-trace or baseline violations to security operations. Include impact, first and latest times, affected and unaffected samples, minimal reproduction, versions, correlation IDs, sanitized logs, actions, and the current [display-resolution-scaling-triage checkpoint] workaround. ## Recovery validation Restore all temporary display_resolution_scaling_triage-trace settings to the approved baseline. Repeat the original operation plus a harmless control, then verify behavior across the relevant boundary: reconnect, relaunch, idle interval, physical movement, or second transaction. Ask the requester to confirm the display_resolution_scaling_triage-trace business result without hidden steps. Check delayed duplicates, secondary errors, and regression elsewhere. Observe proportionately to the original frequency and record objective success, residual constraints, monitoring owner, and [display-resolution-scaling-triage checkpoint] workaround expiry. ## Common failures and display_resolution_scaling_triage-trace limitations Do not reset every component before collecting discriminating evidence. Repeated attempts alter counters, sessions, radio state, leases, or logs and can obscure the initiating event. Never disable protection, install an unapproved component, create a display_resolution_scaling_triage-trace broad exception, expose private data, or continue around heat or damaged hardware. A successful peer narrows scope but cannot prove identical policy, workload, hardware, and environment. ## Closure and limitations Remote support cannot observe every physical display_resolution_scaling_triage-trace or environmental condition relevant to display resolution and scaling. Telemetry may be sampled, privacy controls may hide detail, clocks may differ, and intermittent symptoms may demand a longer window. Vendor-specific interpretation belongs in approved product display_resolution_scaling_triage-trace documentation. If observations conflict or reproduction fails, classify the result as not yet isolated; operational recovery alone is not evidence of root cause. The closure record must let another analyst determine what failed, where the display_resolution_scaling_triage-trace last known success occurred, when behavior began, how impact was measured, what actions altered state, and what evidence supports recovery. Classify the boundary as endpoint-local, connection-path, shared-service, environmental, or unresolved. Distinguish containment from correction. An display_resolution_scaling_triage-trace alternate method needs an approver, owner, protection comparison, expiry, and follow-up when the normal mechanism remains unexplained. ## Topic-specific diagnostics Capture EDID, native timing, refresh rate, color depth, connector, GPU driver and per-monitor DPI. Establish which display_resolution_scaling_triage-trace field changes at the same instant as the symptom. Test the normal managed path first, then one approved comparator; record why the comparator is equivalent and every material difference. A passing status at one layer display_resolution_scaling_triage-trace does not prove the next layer completed. Preserve raw identifiers and timestamps, but redact credentials [display-resolution-scaling-triage checkpoint] and unrelated personal data. For this workflow, distinguish discovery or selection, negotiation, active operation, and teardown or renewal. display_resolution_scaling_triage-trace Name the last stage that completed and the first stage that did not. Repeat only enough times to confirm the discriminator. Restore the supported baseline, challenge recovery across the relevant reconnect or lifecycle transition, and display_resolution_scaling_triage-trace retain both positive and negative observations.

## Canonical sources

- **Microsoft High DPI authority (primary authority):** https://learn.microsoft.com/en-us/windows/win32/hidpi/high-dpi-desktop-application-development-on-windows
- **VESA standards authority (primary authority):** https://vesa.org/vesa-standards/
