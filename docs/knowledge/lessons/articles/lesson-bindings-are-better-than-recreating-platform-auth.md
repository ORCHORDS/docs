# Lesson: Bindings Are Better Than Recreating Platform Auth

When a platform offers first-class service bindings, using them avoids unnecessary network hops and custom credential handling. Prefer the native integration path before rebuilding service-to-service authentication yourself.

Source: Cloudflare Workers Best Practices.