# Lesson: Static Sites Do Not Need Unnecessary Runtime Code

If a workload is purely static, avoid adding server execution just because the platform supports it. Fewer runtime components mean fewer failure modes, permissions, dependencies, and operational responsibilities.

Source: Cloudflare Workers Best Practices.