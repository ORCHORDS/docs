# CI Workflow Permission Minimization

Grant CI jobs only the repository, package, deployment, and cloud permissions they need. Separate untrusted contribution contexts from privileged release workflows and review token scopes explicitly.

Sources: NIST SSDF; official CI platform guidance.