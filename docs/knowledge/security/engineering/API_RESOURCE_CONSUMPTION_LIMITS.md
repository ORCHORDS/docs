# API Resource Consumption Limits

Bound expensive API operations by request size, pagination, concurrency, execution time, upload size, and cost-amplifying parameters. Apply limits at multiple layers and return predictable failure behavior.

Sources: OWASP API Security guidance.