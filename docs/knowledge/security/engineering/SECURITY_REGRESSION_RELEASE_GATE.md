# Security Regression Release Gate

Define a release gate for high-confidence security regressions such as broken authorization, missing validation, exposed secrets, disabled transport protections, or newly failing security tests. Require explicit evidence before overriding the gate.

Sources: NIST SSDF; OWASP testing guidance.