# API Property-Level Authorization

Review which fields a caller may read or modify, especially administrative flags, ownership identifiers, pricing fields, and workflow state. Use explicit schemas and server-side field policies rather than trusting client omission.

Sources: OWASP API Security guidance.