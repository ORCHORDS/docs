# CORS Origin Allowlist Review

Treat CORS as a browser access-control policy, not a server authentication mechanism. Keep allowed origins explicit, minimize credentialed cross-origin access, and review wildcard or reflective origin behavior during architecture changes.

Sources: OWASP CORS guidance.