# Session Idle and Absolute Timeouts

Use both inactivity and maximum-lifetime limits where session risk warrants them. Choose values by data sensitivity, user workflow, device context, and reauthentication cost rather than a single universal number.

Ensure timeout behavior is enforced server-side and clearly communicated to users.

Sources: OWASP Session Management Cheat Sheet.