# Container Runtime Hardening Baseline

Run containers with minimal privileges, constrained filesystem access, controlled capabilities, trusted images, resource limits, and explicit network needs. Avoid using the container boundary as the sole security boundary.

Sources: OWASP Docker Security Cheat Sheet.