# SSRF Destination Control

For server-side fetch features, validate destinations, restrict network reachability, prefer explicit allowlists for known integrations, and protect cloud metadata or internal-only endpoints from user-directed access.

Sources: OWASP SSRF Prevention Cheat Sheet.