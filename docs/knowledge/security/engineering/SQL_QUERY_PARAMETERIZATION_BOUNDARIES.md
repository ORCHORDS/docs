# SQL Query Parameterization Boundaries

Keep untrusted data out of SQL syntax by using prepared statements or safe query builders. Treat dynamic identifiers, sort directions, and fragments separately because ordinary value parameterization does not protect those positions.

Sources: OWASP SQL Injection Prevention Cheat Sheet.