# URL Redirect Target Validation

Prevent open redirects by validating navigation targets against trusted destinations or application routes. Avoid accepting arbitrary absolute URLs from user input when a relative route or allowlisted identifier will work.

Sources: OWASP Unvalidated Redirects and Forwards Cheat Sheet.