# Container Image Minimization

Reduce container images to required runtime components, remove build tools and package caches, use maintained base images, and rebuild regularly so stale dependencies do not persist indefinitely.

Sources: OWASP Docker Security Cheat Sheet; NIST SSDF.