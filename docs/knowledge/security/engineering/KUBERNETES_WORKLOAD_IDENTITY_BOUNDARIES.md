# Kubernetes Workload Identity Boundaries

Assign workloads distinct service identities and narrowly scoped permissions. Avoid broad default service-account access and separate application privileges from cluster-administration capabilities.

Sources: OWASP Kubernetes Security Cheat Sheet.