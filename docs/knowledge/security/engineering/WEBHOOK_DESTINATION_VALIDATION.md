# Webhook Destination Validation

When users configure outbound webhooks, validate scheme and destination, constrain network access, apply timeouts and size limits, and prevent redirects from bypassing network policy. Separate delivery reliability from destination trust.

Sources: OWASP SSRF Prevention guidance.